# Conformance corpus: a state machine without its initial state.
# Clause: CHK-06 (the codec half — the serializer never emits the
# dangling keyword; the refusal itself is the checker's, C109).
state_machine TestReport {
  states {
    DRAFT
    SUBMITTED
  }
  transition DRAFT -> SUBMITTED action submit {
  }
}
