# Conformance corpus: string values with escaped quotes and backslashes.
# Clauses: SER-01 (positive), SER-02 (positive).
role verifier {
  name "The \"chief\" verifier"
}

note N1 {
  text "The path C:\\lab\\reports holds the output"
}
