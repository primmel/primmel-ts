attribute_definition e_max {
  symbol "E_max"
  origin design-fixed
  scope model
}

requirement /req/capacity {
  name "Maximum capacity"
  statement "The instrument shall indicate up to its maximum capacity."
  parameters {
    param e_max: number { description "Maximum capacity." bind "model.parameters.e_max" range { min 0 } }
  }
}
